package apc.appcradle.generics

/**
 * В Kotlin есть возможность использовать reified параметры типа во inline-функциях. Зачем это нужно?
 * Напишите функцию isType, которая принимает объект и возвращает true, если объект является экземпляром указанного типа:
 *
 * println(isType<String>("hello")) // true
 * println(isType<String>(123)) // false
 *
 * Как это работает и почему reified можно использовать только с inline-функциями?
 *
 * Ответ - [ReifiedAnswer]
 *
 */

fun main() {

}
